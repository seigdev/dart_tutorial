void main() {
  // creating maps
  var items = {
    //key     value
    'status': 'false',
    'name': 'moyo',
  };

  print(items['status']);

  // define maps
  var secondItems = Map();
  secondItems['status'] = 'true';
  print(secondItems['status']);
}
