/* 
- dart run has two types of compilation:
- AOT and JIT compilation.
- dart:core  imported by default in every dart program.
- dart:io is used to collect input.
- assert is used for verifying data types or 
  anything basically.
- const does not allow change at runtime.
- using const; values can't be changed during runtime.
- Class() to create object of class.
- ?? used to asign default value.
- ??= used if value is null and want to asign a
  default variable.
- set unique collection of items
- using functions can make codes look understandable
  readable.
- two type of function parameter/argument.
  - positional
  - named
*/


